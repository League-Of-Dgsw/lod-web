const Header = () => {
  return (
    <div className="w-full h-14 flex items-center justify-between px-4 bg-background-surface">
      <div className="flex items-center gap-2">
        <span className="font-black text-base tracking-widest text-text-primary uppercase">League of DGSW</span>
      </div>
    </div>
  );
};

export default Header;
